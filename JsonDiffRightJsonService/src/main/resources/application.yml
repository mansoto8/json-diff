spring:
  application:
    name: json-diff-right-service
server:
  port: 8085
eureka:
  client:
    registry-fetch-interval-seconds: 15
    register-with-eureka: true
    fetch-registry: true
    service-url: 
      default-zone: http://localhost:8761/eureka/
